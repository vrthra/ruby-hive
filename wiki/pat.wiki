pat

pat is the trait that ties up Pat with Hive.

actions: start flush show size

start:  executes all the test cases provided in all.seq file.
!do $pat:start[-s all.seq]



show:  show outputs the logs created during the last pat run.

size:  Size returns the logsize

flush:  flush cleares them